# NextAuth.js Configuration
# Generate a secret with: openssl rand -base64 32
AUTH_SECRET=your-auth-secret-here

# Application URL (for NextAuth.js)
NEXTAUTH_URL=http://localhost:3000

# Supabase Configuration (REQUIRED for database features)
# 1. Create a free Supabase project at https://supabase.com
# 2. Go to Project Settings > API
# 3. Copy the URL and anon/public key
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Admin Configuration (REQUIRED for production)
# Password for the default admin user (info@securesitescan.com)
# The admin user will be created on first startup with this password
# Use a strong password with at least 12 characters
ADMIN_PASSWORD=your-secure-admin-password-here

# Email Configuration (OPTIONAL - for contact form)
# Zoho Mail SMTP credentials for sending contact form emails
ZOHO_EMAIL_USER=noreply@securesitescan.com
ZOHO_EMAIL_PASSWORD=your-zoho-app-password

# GitHub Configuration (OPTIONAL - for enhanced GitHub API access)
# Personal access token for higher rate limits on GitHub API
GITHUB_TOKEN=your-github-personal-access-token

# Stripe Configuration (REQUIRED for payments)
# 1. Create a Stripe account at https://stripe.com
# 2. Go to Developers > API keys
# 3. Copy your Secret key (starts with sk_)
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key

# Stripe Price IDs (REQUIRED for subscriptions)
# Create products and prices in Stripe Dashboard > Products
# Copy the price IDs (starts with price_)
STRIPE_STARTER_PRICE_ID=price_your-starter-price-id
STRIPE_PRO_PRICE_ID=price_your-pro-price-id

# Stripe Webhook Secret (REQUIRED for payment confirmation)
# 1. Go to Developers > Webhooks
# 2. Add endpoint: https://yourdomain.com/api/stripe/webhook
# 3. Select events: checkout.session.completed, customer.subscription.*
# 4. Copy the signing secret (starts with whsec_)
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# After setting up Supabase:
# 1. Go to SQL Editor in Supabase dashboard
# 2. Run the SQL from supabase/schema.sql to create tables
